# Gemini is optional for local UI development. Add it to enable live tool-using responses.
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.0-flash

# Optional: override the SQLite file location. The default is ./data/property-copilot.db
DATABASE_PATH=./data/property-copilot.db
